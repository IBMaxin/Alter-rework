# CLAUDE.md

Alter-rework is an OSRS-style RuneScape private server (RSMod fork) in Kotlin 2.0.20, targeting OSRS cache b228. All game content lives in Kotlin plugins under `game-plugins/`.

**Read `AGENTS.md` in the repo root for all project rules, build commands, constraints, and coding conventions.** That file is the single source of truth — this file exists only because Claude Code does not read AGENTS.md.
